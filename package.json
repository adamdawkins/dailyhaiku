{
  "name": "adams-haikus",
  "description": "Adam's Daily Haikus",
  "version": "1.0.0",
	"repository": {
		"type": "git",
		"url": "https://github.com/adamdawkins/dailyhaiku"
	},
  "author": "Adam Dawkins <adam@dragondrop.uk>",
  "dependencies": {
    "gatsby": "^1.0.1",
    "gatsby-link": "^1.0.1",
    "gatsby-plugin-catch-links": "^1.0.1",
    "gatsby-plugin-react-helmet": "^1.0.1",
    "gatsby-remark-smartypants": "^1.0.1",
    "gatsby-source-filesystem": "^1.0.1",
    "gatsby-transformer-remark": "^1.1.0",
    "react-typekit": "^1.1.0"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
		"start": "npm run develop",
    "build": "gatsby build",
    "deploy": "rm -rf public/*.html && gatsby build && now public",
    "develop": "gatsby develop",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write \"pages/*.js\" \"utils/*.js\" \"wrappers/*.js\" \"html.js\"",
		"changelog": "$(yarn bin)/changelog",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "changelog": "^1.4.0"
  }
}
